---
phase: 09-containerized-integration-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/k8s/snmp-collector/configmap.yaml
  - deploy/k8s/snmp-collector/deployment.yaml
  - deploy/k8s/snmp-collector/service.yaml
autonomous: true

must_haves:
  truths:
    - "SnmpCollector ConfigMap provides appsettings.Production.json with Lease.Namespace=simetra, dummy device for readiness, and OTLP endpoint pointing to otel-collector:4317"
    - "SnmpCollector Deployment runs 3 replicas with serviceAccountName=simetra-sa and imagePullPolicy=Never for local Docker build"
    - "Each pod receives its own name via Downward API into Site__PodIdentity for leader election identity"
    - "Health probes (startup, readiness, liveness) are configured on port 8080 matching Phase 8 endpoints"
    - "SnmpCollector Service exposes health port 8080 as ClusterIP"
  artifacts:
    - path: "deploy/k8s/snmp-collector/configmap.yaml"
      provides: "SnmpCollector K8s appsettings overlay"
      contains: "snmp-collector-config"
    - path: "deploy/k8s/snmp-collector/deployment.yaml"
      provides: "SnmpCollector 3-replica deployment with probes and downward API"
      contains: "replicas: 3"
    - path: "deploy/k8s/snmp-collector/service.yaml"
      provides: "SnmpCollector ClusterIP service"
      contains: "app: snmp-collector"
  key_links:
    - from: "deploy/k8s/snmp-collector/deployment.yaml"
      to: "deploy/k8s/snmp-collector/configmap.yaml"
      via: "volumeMount subPath appsettings.k8s.json"
      pattern: "snmp-collector-config"
    - from: "deploy/k8s/snmp-collector/configmap.yaml"
      to: "OTel Collector service"
      via: "Otlp.Endpoint"
      pattern: "http://otel-collector:4317"
    - from: "deploy/k8s/snmp-collector/deployment.yaml"
      to: "deploy/k8s/serviceaccount.yaml"
      via: "serviceAccountName"
      pattern: "simetra-sa"
---

<objective>
Create SnmpCollector-specific K8s manifests (ConfigMap, Deployment, Service) in a new `deploy/k8s/snmp-collector/` subdirectory.

Purpose: SnmpCollector needs its own K8s manifests separate from the existing Simetra manifests. These manifests configure 3 replicas with leader election, health probes, pod identity via Downward API, and a dummy device to satisfy the readiness probe. The existing Simetra manifests remain intact for reference.

Output: Three manifest files in `deploy/k8s/snmp-collector/` ready for `kubectl apply`.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-containerized-integration-testing/09-RESEARCH.md
@deploy/k8s/deployment.yaml
@deploy/k8s/service.yaml
@deploy/k8s/configmap.yaml
@src/SnmpCollector/appsettings.json
@src/SnmpCollector/Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SnmpCollector ConfigMap</name>
  <files>
    deploy/k8s/snmp-collector/configmap.yaml
  </files>
  <action>
    Create directory `deploy/k8s/snmp-collector/` and create `configmap.yaml`.

    The ConfigMap must override all K8s-specific settings that differ from `appsettings.json` defaults. The `data` key is `appsettings.k8s.json` (mounted as `appsettings.Production.json` by the Deployment).

    ConfigMap content:
    ```yaml
    apiVersion: v1
    kind: ConfigMap
    metadata:
      name: snmp-collector-config
      namespace: simetra
    data:
      appsettings.k8s.json: |
        {
          "Site": {
            "Name": "site-lab-k8s"
          },
          "Otlp": {
            "Endpoint": "http://otel-collector:4317",
            "ServiceName": "snmp-collector"
          },
          "SnmpListener": {
            "BindAddress": "0.0.0.0",
            "Port": 10162,
            "CommunityString": "public"
          },
          "Lease": {
            "Name": "snmp-collector-leader",
            "Namespace": "simetra",
            "DurationSeconds": 15,
            "RenewIntervalSeconds": 10
          },
          "CorrelationJob": {
            "IntervalSeconds": 30
          },
          "Liveness": {
            "GraceMultiplier": 2.0
          },
          "Channels": {
            "BoundedCapacity": 100
          },
          "Devices": [
            {
              "Name": "dummy-device-01",
              "IpAddress": "127.0.0.1",
              "MetricPolls": []
            }
          ],
          "Logging": {
            "EnableConsole": true
          }
        }
    ```

    CRITICAL config decisions and WHY:
    - `Lease.Namespace: "simetra"` — RBAC grants lease access to simetra-sa in the simetra namespace only. The appsettings.json default is "default" which would cause 403 Forbidden on lease operations (Research Pattern 6).
    - `Devices` contains one dummy device with empty MetricPolls — ReadinessHealthCheck requires `DeviceChannelManager.DeviceNames.Count > 0`. Without at least one device, readiness always fails and K8s never routes traffic (Research Pattern 7).
    - `SnmpListener.Port: 10162` — non-privileged port matching Dockerfile EXPOSE.
    - `Logging.EnableConsole: true` — enables `kubectl logs` visibility.
  </action>
  <verify>
    - File exists at `deploy/k8s/snmp-collector/configmap.yaml`
    - Contains `"Namespace": "simetra"` in Lease section
    - Contains `"dummy-device-01"` in Devices array
    - Contains `"Endpoint": "http://otel-collector:4317"` in Otlp section
  </verify>
  <done>
    ConfigMap provides a complete appsettings.Production.json overlay for K8s with correct lease namespace, dummy device for readiness, and OTel Collector endpoint.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SnmpCollector Deployment and Service</name>
  <files>
    deploy/k8s/snmp-collector/deployment.yaml
    deploy/k8s/snmp-collector/service.yaml
  </files>
  <action>
    Create `deploy/k8s/snmp-collector/deployment.yaml` based on the existing Simetra `deploy/k8s/deployment.yaml` template with these adaptations:

    Deployment spec:
    - `metadata.name: snmp-collector`, `namespace: simetra`, `labels.app: snmp-collector`
    - `spec.replicas: 3` (leader election test requires multiple replicas)
    - `spec.selector.matchLabels.app: snmp-collector`
    - `serviceAccountName: simetra-sa` (reuse existing SA — has RBAC for leases in simetra namespace)
    - `terminationGracePeriodSeconds: 30` (matches Phase 8 graceful shutdown budget)
    - Container:
      - `name: snmp-collector`
      - `image: snmp-collector:local` (built locally with `docker build`)
      - `imagePullPolicy: Never` (Docker Desktop local image, not registry)
      - Ports: `8080/tcp` (health) only. No SNMP port — no device traffic in Phase 9.
      - Environment variables:
        - `ASPNETCORE_ENVIRONMENT: Production` (activates appsettings.Production.json merge)
        - `Site__PodIdentity` from Downward API `metadata.name` (K8sLeaseElection holder identity)
      - Resources: requests cpu=100m mem=128Mi, limits cpu=500m mem=256Mi
      - Volume mount: `snmp-collector-config` ConfigMap -> `/app/appsettings.Production.json` via subPath `appsettings.k8s.json`
      - Startup probe: httpGet `/healthz/startup` port health, initialDelaySeconds=5, periodSeconds=3, failureThreshold=10
      - Readiness probe: httpGet `/healthz/ready` port health, periodSeconds=10, failureThreshold=3
      - Liveness probe: httpGet `/healthz/live` port health, periodSeconds=15, failureThreshold=3, timeoutSeconds=5

    Create `deploy/k8s/snmp-collector/service.yaml`:
    - ClusterIP service, `name: snmp-collector`, `namespace: simetra`
    - Selector: `app: snmp-collector`
    - Port: `health` 8080 TCP -> targetPort health
    - No SNMP port (no device traffic in Phase 9)

    WHY no SNMP port: Phase 9 validates runtime/pipeline metrics only. No SNMP devices are configured (dummy device has empty MetricPolls). The SNMP listener binds internally but receives no external traffic.
  </action>
  <verify>
    - Files exist at `deploy/k8s/snmp-collector/deployment.yaml` and `deploy/k8s/snmp-collector/service.yaml`
    - Deployment has `replicas: 3`, `imagePullPolicy: Never`, `image: snmp-collector:local`
    - Deployment has `Site__PodIdentity` env var from Downward API `metadata.name`
    - Deployment has all three health probes (startup, readiness, liveness)
    - Service is ClusterIP with port 8080
    - Neither file contains SNMP port (162 or 10162)
  </verify>
  <done>
    SnmpCollector Deployment (3 replicas with probes, downward API, local image) and Service (ClusterIP health port) are ready for kubectl apply. Deployment reuses simetra-sa ServiceAccount for lease RBAC.
  </done>
</task>

</tasks>

<verification>
- All three manifests are syntactically valid YAML
- All manifests target `namespace: simetra`
- ConfigMap key `appsettings.k8s.json` is valid JSON
- Deployment volume mount references `snmp-collector-config` ConfigMap
- Deployment image is `snmp-collector:local` with `imagePullPolicy: Never`
- Downward API injects pod name into `Site__PodIdentity`
</verification>

<success_criteria>
Three SnmpCollector K8s manifests exist in `deploy/k8s/snmp-collector/` and are ready for deployment. ConfigMap has correct lease namespace and dummy device. Deployment has 3 replicas with health probes. Service exposes health port.
</success_criteria>

<output>
After completion, create `.planning/phases/09-containerized-integration-testing/09-02-SUMMARY.md`
</output>
