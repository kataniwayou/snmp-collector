---
phase: 10-metrics
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/Configuration/DeviceOptions.cs
  - src/SnmpCollector/Pipeline/DeviceInfo.cs
  - src/SnmpCollector/Pipeline/DeviceRegistry.cs
  - src/SnmpCollector/Jobs/MetricPollJob.cs
  - src/SnmpCollector/Configuration/Validators/DevicesOptionsValidator.cs
  - src/SnmpCollector/appsettings.Development.json
  - deploy/k8s/snmp-collector/configmap.yaml
  - tests/SnmpCollector.Tests/Jobs/MetricPollJobTests.cs
  - tests/SnmpCollector.Tests/Pipeline/DeviceRegistryTests.cs
  - tests/SnmpCollector.Tests/Pipeline/PipelineIntegrationTests.cs
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "DeviceOptions has no CommunityString property — Name alone determines community"
    - "MetricPollJob derives community string via CommunityStringHelper.DeriveFromDeviceName(device.Name)"
    - "DeviceInfo record has no CommunityString parameter"
    - "DevicesOptionsValidator has no CommunityString validation rules"
    - "Config files (appsettings, configmaps) have no CommunityString per device"
    - "All tests pass after removal"
  artifacts:
    - path: "src/SnmpCollector/Configuration/DeviceOptions.cs"
      provides: "Device config without CommunityString"
      contains: "Name"
    - path: "src/SnmpCollector/Pipeline/DeviceInfo.cs"
      provides: "Runtime device record without CommunityString"
    - path: "src/SnmpCollector/Jobs/MetricPollJob.cs"
      provides: "Poll job deriving community from device name"
      contains: "CommunityStringHelper.DeriveFromDeviceName"
  key_links:
    - from: "src/SnmpCollector/Jobs/MetricPollJob.cs"
      to: "src/SnmpCollector/Pipeline/CommunityStringHelper.cs"
      via: "DeriveFromDeviceName call"
      pattern: "CommunityStringHelper\\.DeriveFromDeviceName"
    - from: "src/SnmpCollector/Pipeline/DeviceRegistry.cs"
      to: "src/SnmpCollector/Pipeline/DeviceInfo.cs"
      via: "DeviceInfo constructor without CommunityString"
      pattern: "new DeviceInfo\\("
---

<objective>
Remove redundant CommunityString from per-device configuration. Since community strings always follow the Simetra.{DeviceName} convention, the CommunityString property is derivable from Name and should not be a separate config field.

Purpose: Eliminate config redundancy — users should not have to type "Simetra.npb-core-01" when they already typed "npb-core-01" as the Name. The convention is already enforced by CommunityStringHelper.
Output: DeviceOptions/DeviceInfo without CommunityString, MetricPollJob deriving community at runtime, clean config files.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-metrics/10-UAT-final.md
@src/SnmpCollector/Pipeline/CommunityStringHelper.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove CommunityString from config model, runtime record, registry, and validator</name>
  <files>
    src/SnmpCollector/Configuration/DeviceOptions.cs
    src/SnmpCollector/Pipeline/DeviceInfo.cs
    src/SnmpCollector/Pipeline/DeviceRegistry.cs
    src/SnmpCollector/Configuration/Validators/DevicesOptionsValidator.cs
  </files>
  <action>
    1. **DeviceOptions.cs** — Remove the `CommunityString` property and its XML doc comment entirely. The class should have: Name, IpAddress, Port, MetricPolls only.

    2. **DeviceInfo.cs** — Remove the `CommunityString` parameter from the record definition. The record should be: `DeviceInfo(string Name, string IpAddress, int Port, IReadOnlyList<MetricPollInfo> PollGroups)`. Update the XML doc to remove the CommunityString param doc.

    3. **DeviceRegistry.cs** — Update the `DeviceInfo` constructor call on line 45 to remove the `d.CommunityString` argument:
       `var info = new DeviceInfo(d.Name, d.IpAddress, d.Port, pollGroups);`
       Also remove the XML doc mention of "Community strings are configured per-device" from the class summary.

    4. **DevicesOptionsValidator.cs** — Remove the entire CommunityString validation block (lines 53-59 in ValidateDevice): the `if (string.IsNullOrWhiteSpace(device.CommunityString))` and the `else if (!device.CommunityString.StartsWith("Simetra."))` checks. No replacement needed — the convention is now implicit from Name.
  </action>
  <verify>
    `dotnet build src/SnmpCollector/SnmpCollector.csproj` compiles with 0 errors. Grep for `CommunityString` in src/SnmpCollector/ returns only CommunityStringHelper.cs (the helper itself, which is still needed for trap path).
  </verify>
  <done>CommunityString removed from DeviceOptions, DeviceInfo, DeviceRegistry, and DevicesOptionsValidator. Only CommunityStringHelper.cs references remain in src/.</done>
</task>

<task type="auto">
  <name>Task 2: Update MetricPollJob to derive community string and fix all tests and config files</name>
  <files>
    src/SnmpCollector/Jobs/MetricPollJob.cs
    src/SnmpCollector/appsettings.Development.json
    deploy/k8s/snmp-collector/configmap.yaml
    tests/SnmpCollector.Tests/Jobs/MetricPollJobTests.cs
    tests/SnmpCollector.Tests/Pipeline/DeviceRegistryTests.cs
    tests/SnmpCollector.Tests/Pipeline/PipelineIntegrationTests.cs
  </files>
  <action>
    1. **MetricPollJob.cs** — Change line 83 from:
       `var community = new OctetString(device.CommunityString);`
       to:
       `var community = new OctetString(CommunityStringHelper.DeriveFromDeviceName(device.Name));`
       Add `using SnmpCollector.Pipeline;` if not already present (it is — DeviceInfo is in that namespace).

    2. **appsettings.Development.json** — Remove `"CommunityString": "Simetra.npb-core-01"` and `"CommunityString": "Simetra.obp-edge-01"` lines from both device entries in the Devices array.

    3. **deploy/k8s/snmp-collector/configmap.yaml** — Remove `"CommunityString": "Simetra.dummy-device-01"` from the dummy device entry in the Devices array.
       NOTE: Do NOT touch deploy/k8s/production/configmap.yaml — that is the old Simetra project template, not the SnmpCollector project. It has a different config model (DeviceType, etc.) and is unrelated.

    4. **MetricPollJobTests.cs** — Three changes:
       a. Remove `private const string DeviceCommunity = "Simetra.test-router";` (line 36).
       b. Update `MakeDevice` helper (line 85): change `new DeviceInfo(DeviceName, DeviceIp, DevicePort, DeviceCommunity, [pollGroup])` to `new DeviceInfo(DeviceName, DeviceIp, DevicePort, [pollGroup])`.
       c. Update test `Execute_UsesDevicePortAndCommunityString` (line 187): change `new DeviceInfo("custom-device", "10.0.0.99", 1161, "Simetra.custom-device", [pollGroup])` to `new DeviceInfo("custom-device", "10.0.0.99", 1161, [pollGroup])`. The assertion on line 202 `Assert.Equal("Simetra.custom-device", snmpClient.LastCommunity!.ToString())` should REMAIN — it now verifies the derivation works. Rename the test method to `Execute_UsesDevicePortAndDerivesCommunityString` and update the comment to "Test 3: Device Port used and CommunityString derived from device name".

    5. **DeviceRegistryTests.cs** — Remove `CommunityString = "Simetra.npb-core-01"` (line 24) and `CommunityString = "Simetra.obp-edge-01"` (line 38) from the two DeviceOptions in TwoDeviceOptions().

    6. **PipelineIntegrationTests.cs** — Remove `CommunityString = "Simetra." + KnownDevice` (line 62) from the DeviceOptions in the test setup.
  </action>
  <verify>
    Run `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` — all tests pass with 0 failures.
    Run `grep -r "CommunityString" src/SnmpCollector/ tests/SnmpCollector.Tests/ --include="*.cs"` — only hits should be in CommunityStringHelper.cs, SnmpTrapListenerService.cs (trap path), and SnmpTrapListenerServiceTests.cs (trap tests). Zero hits in DeviceOptions, DeviceInfo, DeviceRegistry, MetricPollJob, or validator.
  </verify>
  <done>MetricPollJob derives community from device.Name. All config files have no per-device CommunityString. All tests updated and passing. The only CommunityString references remaining are in the trap listener path (CommunityStringHelper, SnmpTrapListenerService) where they correctly handle incoming trap community strings.</done>
</task>

</tasks>

<verification>
1. `dotnet build src/SnmpCollector/SnmpCollector.csproj` — 0 errors, 0 warnings
2. `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj` — all tests pass
3. `grep -rn "CommunityString" src/SnmpCollector/ --include="*.cs"` — only CommunityStringHelper.cs and SnmpTrapListenerService.cs
4. `grep -rn "CommunityString" tests/SnmpCollector.Tests/ --include="*.cs"` — only SnmpTrapListenerServiceTests.cs
5. `grep -rn "CommunityString" src/SnmpCollector/appsettings*.json deploy/k8s/snmp-collector/` — zero hits
</verification>

<success_criteria>
- CommunityString property fully removed from DeviceOptions, DeviceInfo, DeviceRegistry
- CommunityString validation removed from DevicesOptionsValidator
- MetricPollJob uses CommunityStringHelper.DeriveFromDeviceName(device.Name) instead of device.CommunityString
- All config files (appsettings.Development.json, k8s configmap) no longer specify per-device CommunityString
- All tests pass
- CommunityStringHelper.cs itself is untouched (still needed for trap path)
</success_criteria>

<output>
After completion, create `.planning/phases/10-metrics/10-08-SUMMARY.md`
</output>
