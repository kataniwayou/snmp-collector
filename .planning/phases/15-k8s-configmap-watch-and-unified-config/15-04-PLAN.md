---
phase: 15-k8s-configmap-watch-and-unified-config
plan: 04
type: execute
wave: 3
depends_on: ["15-01"]
files_modified:
  - deploy/k8s/configmap.yaml
  - deploy/k8s/rbac.yaml
  - deploy/k8s/production/rbac.yaml
autonomous: true

must_haves:
  truths:
    - "ConfigMap has a single simetra-config.json key containing all 92 OIDs and 2 devices with JSONC comments"
    - "Separate oidmap-obp.json, oidmap-npb.json, and devices.json keys are removed from ConfigMap"
    - "RBAC Role grants configmaps get/list/watch permissions to simetra-sa ServiceAccount"
    - "appsettings.k8s.json key is preserved (app-level settings, not device config)"
    - "OidMap section in ConfigMap has no OidMap wrapper -- OidMap is the top-level key in the unified JSON"
  artifacts:
    - path: "deploy/k8s/configmap.yaml"
      provides: "ConfigMap with unified simetra-config.json key"
      contains: "simetra-config.json"
    - path: "deploy/k8s/rbac.yaml"
      provides: "RBAC with configmaps read/watch permissions"
      contains: "configmaps"
  key_links:
    - from: "deploy/k8s/configmap.yaml"
      to: "src/SnmpCollector/Services/ConfigMapWatcherService.cs"
      via: "ConfigMap key name matches ConfigKey constant"
      pattern: "simetra-config.json"
    - from: "deploy/k8s/rbac.yaml"
      to: "src/SnmpCollector/Services/ConfigMapWatcherService.cs"
      via: "RBAC permits configmaps watch for watcher service"
      pattern: "configmaps"
---

<objective>
Update K8s manifests: unified ConfigMap key and RBAC permissions for ConfigMap watching.

Purpose: Replace the three separate ConfigMap keys (oidmap-obp.json, oidmap-npb.json, devices.json) with a single simetra-config.json key containing JSONC-documented OID maps and device entries. Add configmaps read/list/watch RBAC permissions so ConfigMapWatcherService can watch for changes.

Output: Updated configmap.yaml and rbac.yaml manifests ready for kubectl apply.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-k8s-configmap-watch-and-unified-config/15-CONTEXT.md
@deploy/k8s/configmap.yaml
@deploy/k8s/rbac.yaml
@deploy/k8s/production/rbac.yaml
@src/SnmpCollector/config/oidmap-obp.json
@src/SnmpCollector/config/oidmap-npb.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace ConfigMap with unified simetra-config.json key</name>
  <files>deploy/k8s/configmap.yaml</files>
  <action>
    Rewrite `deploy/k8s/configmap.yaml`:

    1. Keep the `appsettings.k8s.json` key exactly as-is (lines 7-32). It contains app-level settings (Site, Otlp, SnmpListener, HeartbeatJob, Liveness, Logging) that are separate from device config. Remove the empty `"OidMap": {}` from appsettings.k8s.json since OID maps now live in the unified key.

    2. Remove the three separate keys:
       - `oidmap-obp.json` (the entire key and its JSON content)
       - `oidmap-npb.json` (the entire key and its JSON content)
       - `devices.json` (the entire key and its JSON content)

    3. Add a single `simetra-config.json` key containing JSONC content:
       ```yaml
       simetra-config.json: |
         {
           // ===========================================================================
           // Simetra Unified Device Configuration
           // This file is the single source of truth for OID maps and device definitions.
           // Changes to this ConfigMap are detected via K8s API watch and applied at runtime
           // without pod restart.
           // ===========================================================================

           "OidMap": {
             // ---- OBP (Optical Bypass) OIDs ----
             // Link state: INTEGER | 1=active, 2=bypass, 3=fault
             "1.3.6.1.4.1.47477.10.21.1.3.1.0": "obp_link_state_L1",
             // Channel: INTEGER | DWDM channel number (1-80)
             "1.3.6.1.4.1.47477.10.21.1.3.4.0": "obp_channel_L1",
             // Optical power: INTEGER | dBm x100 (divide by 100 for actual dBm)
             "1.3.6.1.4.1.47477.10.21.1.3.10.0": "obp_r1_power_L1",
             "1.3.6.1.4.1.47477.10.21.1.3.11.0": "obp_r2_power_L1",
             "1.3.6.1.4.1.47477.10.21.1.3.12.0": "obp_r3_power_L1",
             "1.3.6.1.4.1.47477.10.21.1.3.13.0": "obp_r4_power_L1",

             "1.3.6.1.4.1.47477.10.21.2.3.1.0": "obp_link_state_L2",
             "1.3.6.1.4.1.47477.10.21.2.3.4.0": "obp_channel_L2",
             "1.3.6.1.4.1.47477.10.21.2.3.10.0": "obp_r1_power_L2",
             "1.3.6.1.4.1.47477.10.21.2.3.11.0": "obp_r2_power_L2",
             "1.3.6.1.4.1.47477.10.21.2.3.12.0": "obp_r3_power_L2",
             "1.3.6.1.4.1.47477.10.21.2.3.13.0": "obp_r4_power_L2",

             "1.3.6.1.4.1.47477.10.21.3.3.1.0": "obp_link_state_L3",
             "1.3.6.1.4.1.47477.10.21.3.3.4.0": "obp_channel_L3",
             "1.3.6.1.4.1.47477.10.21.3.3.10.0": "obp_r1_power_L3",
             "1.3.6.1.4.1.47477.10.21.3.3.11.0": "obp_r2_power_L3",
             "1.3.6.1.4.1.47477.10.21.3.3.12.0": "obp_r3_power_L3",
             "1.3.6.1.4.1.47477.10.21.3.3.13.0": "obp_r4_power_L3",

             "1.3.6.1.4.1.47477.10.21.4.3.1.0": "obp_link_state_L4",
             "1.3.6.1.4.1.47477.10.21.4.3.4.0": "obp_channel_L4",
             "1.3.6.1.4.1.47477.10.21.4.3.10.0": "obp_r1_power_L4",
             "1.3.6.1.4.1.47477.10.21.4.3.11.0": "obp_r2_power_L4",
             "1.3.6.1.4.1.47477.10.21.4.3.12.0": "obp_r3_power_L4",
             "1.3.6.1.4.1.47477.10.21.4.3.13.0": "obp_r4_power_L4",

             // ---- NPB (Network Packet Broker) OIDs ----
             // System health
             // CPU utilization: INTEGER | percentage (0-100)
             "1.3.6.1.4.1.47477.100.1.1.0": "npb_cpu_util",
             // Memory utilization: INTEGER | percentage (0-100)
             "1.3.6.1.4.1.47477.100.1.2.0": "npb_mem_util",
             // System temperature: INTEGER | degrees Celsius
             "1.3.6.1.4.1.47477.100.1.3.0": "npb_sys_temp",
             // Uptime: INTEGER | seconds since boot
             "1.3.6.1.4.1.47477.100.1.4.0": "npb_uptime",

             // Per-port metrics (P1-P8)
             // Port status: INTEGER | 1=up, 2=down
             // Octets/packets: Counter64 | cumulative bytes/packets
             // Errors/drops: Counter64 | cumulative error/drop counts
             ... (ALL 64 per-port NPB OIDs from oidmap-npb.json -- 8 ports x 8 metrics each)
           },

           "Devices": [
             {
               "Name": "OBP-01",
               "IpAddress": "obp-simulator.simetra.svc.cluster.local",
               "Port": 161,
               "CommunityString": "Simetra.OBP-01",
               "MetricPolls": [
                 {
                   "IntervalSeconds": 10,
                   "Oids": [
                     ... (all 24 OBP OIDs)
                   ]
                 }
               ]
             },
             {
               "Name": "NPB-01",
               "IpAddress": "npb-simulator.simetra.svc.cluster.local",
               "Port": 161,
               "CommunityString": "Simetra.NPB-01",
               "MetricPolls": [
                 {
                   "IntervalSeconds": 10,
                   "Oids": [
                     ... (all 68 NPB OIDs)
                   ]
                 }
               ]
             }
           ]
         }
       ```

    CRITICAL: Copy ALL 92 OID entries exactly from the existing configmap.yaml oidmap-obp.json (24 entries) and oidmap-npb.json (68 entries). Copy ALL OID strings into the device MetricPolls arrays exactly as they appear in the existing devices.json key. Do NOT miss any OIDs.

    Add JSONC comments for documentation:
    - Section headers for OBP vs NPB
    - Brief inline comments for OID types (link state, channel, power, port status, traffic counters, etc.)
    - Header comment explaining the file is the single source of truth
  </action>
  <verify>
    - Validate YAML syntax: `python -c "import yaml; yaml.safe_load(open('deploy/k8s/configmap.yaml'))"` (if python available) or visual inspection.
    - Count OID entries in the simetra-config.json key: must have exactly 92 OidMap entries (24 OBP + 68 NPB).
    - Verify NO oidmap-obp.json, oidmap-npb.json, or devices.json keys remain.
    - Verify appsettings.k8s.json key is preserved (without empty OidMap).
    - Verify both device entries (OBP-01, NPB-01) are present with correct K8s DNS addresses.
  </verify>
  <done>
    ConfigMap has single simetra-config.json key with all 92 OIDs, 2 devices, and JSONC documentation. Legacy separate keys removed. appsettings.k8s.json preserved for app settings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update RBAC for ConfigMap watch permissions and cleanup legacy config files</name>
  <files>
    deploy/k8s/rbac.yaml
    deploy/k8s/production/rbac.yaml
  </files>
  <action>
    1. Update `deploy/k8s/rbac.yaml`:
       - Rename the Role from `simetra-lease-role` to `simetra-role` (it now covers more than just leases).
       - Add a second rule for ConfigMap access:
         ```yaml
         - apiGroups: [""]
           resources: ["configmaps"]
           verbs: ["get", "list", "watch"]
         ```
       - Update the RoleBinding to reference the renamed role:
         - Change `name: simetra-lease-binding` to `name: simetra-binding`
         - Change `roleRef.name: simetra-lease-role` to `roleRef.name: simetra-role`
       - Keep the existing leases rule unchanged.

    2. Update `deploy/k8s/production/rbac.yaml` (if it exists):
       - Apply the same changes as the base rbac.yaml.
       - If production/rbac.yaml doesn't exist or is identical to base, skip this step.

    3. Do NOT delete `src/SnmpCollector/config/oidmap-obp.json` or `src/SnmpCollector/config/oidmap-npb.json` yet -- they serve as reference. They are no longer loaded by Program.cs (removed in Plan 03). They can be cleaned up in a future commit if desired.
  </action>
  <verify>
    - Verify rbac.yaml has both `leases` and `configmaps` rules.
    - Verify Role name is `simetra-role` and RoleBinding references match.
    - Validate YAML syntax.
  </verify>
  <done>
    RBAC grants configmaps get/list/watch to simetra-sa. Role renamed from simetra-lease-role to simetra-role. Both base and production RBAC updated.
  </done>
</task>

</tasks>

<verification>
- configmap.yaml has exactly one device config key (simetra-config.json) with 92 OIDs
- No oidmap-obp.json, oidmap-npb.json, or devices.json keys in configmap.yaml
- rbac.yaml has configmaps get/list/watch rule
- Both YAML files are syntactically valid
</verification>

<success_criteria>
K8s manifests are ready for deployment. ConfigMap has a single unified key with JSONC documentation. RBAC permits ConfigMap watching. Legacy separate keys are removed from the ConfigMap.
</success_criteria>

<output>
After completion, create `.planning/phases/15-k8s-configmap-watch-and-unified-config/15-04-SUMMARY.md`
</output>
