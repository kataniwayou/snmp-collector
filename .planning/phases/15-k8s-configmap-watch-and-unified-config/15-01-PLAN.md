---
phase: 15-k8s-configmap-watch-and-unified-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SnmpCollector/Configuration/SimetraConfigModel.cs
  - src/SnmpCollector/Pipeline/OidMapService.cs
  - src/SnmpCollector/Pipeline/IOidMapService.cs
  - src/SnmpCollector/Pipeline/DeviceRegistry.cs
  - src/SnmpCollector/Pipeline/IDeviceRegistry.cs
  - src/SnmpCollector/Pipeline/JobIntervalRegistry.cs
  - src/SnmpCollector/Pipeline/IJobIntervalRegistry.cs
  - src/SnmpCollector/Pipeline/ILivenessVectorService.cs
  - src/SnmpCollector/Pipeline/LivenessVectorService.cs
  - tests/SnmpCollector.Tests/Pipeline/OidMapServiceTests.cs
  - tests/SnmpCollector.Tests/Pipeline/DeviceRegistryTests.cs
autonomous: true

must_haves:
  truths:
    - "OidMapService accepts initial OID map data without IOptionsMonitor and supports UpdateMap for atomic swap"
    - "DeviceRegistry supports Reload with new device list and atomic FrozenDictionary swap"
    - "JobIntervalRegistry supports Unregister to remove entries for deleted jobs"
    - "LivenessVectorService supports Remove to clean up stamps for deleted jobs"
    - "SimetraConfigModel deserializes unified JSON with OidMap and Devices sections"
    - "All existing tests pass after refactoring"
  artifacts:
    - path: "src/SnmpCollector/Configuration/SimetraConfigModel.cs"
      provides: "Unified config POCO with OidMap dictionary and Devices list"
      contains: "class SimetraConfigModel"
    - path: "src/SnmpCollector/Pipeline/OidMapService.cs"
      provides: "OID resolution with UpdateMap method instead of IOptionsMonitor"
      contains: "UpdateMap"
    - path: "src/SnmpCollector/Pipeline/DeviceRegistry.cs"
      provides: "Mutable device registry with atomic swap on Reload"
      contains: "ReloadAsync"
    - path: "src/SnmpCollector/Pipeline/JobIntervalRegistry.cs"
      provides: "Job interval registry with Unregister support"
      contains: "Unregister"
    - path: "src/SnmpCollector/Pipeline/LivenessVectorService.cs"
      provides: "Liveness vector with Remove support"
      contains: "Remove"
  key_links:
    - from: "src/SnmpCollector/Pipeline/OidMapService.cs"
      to: "src/SnmpCollector/Configuration/SimetraConfigModel.cs"
      via: "UpdateMap accepts Dictionary<string, string> from parsed config"
      pattern: "UpdateMap.*Dictionary"
    - from: "src/SnmpCollector/Pipeline/DeviceRegistry.cs"
      to: "src/SnmpCollector/Configuration/DeviceOptions.cs"
      via: "ReloadAsync accepts List<DeviceOptions>"
      pattern: "ReloadAsync.*List<DeviceOptions>"
---

<objective>
Create the unified config model and make all registries mutable for live reload.

Purpose: OidMapService, DeviceRegistry, JobIntervalRegistry, and LivenessVectorService must support runtime updates so the ConfigMap watcher (Plan 02) can reload them without pod restart. This is the foundation for all dynamic reload behavior.

Output: SimetraConfigModel POCO, refactored OidMapService (no IOptionsMonitor), mutable DeviceRegistry with atomic swap, JobIntervalRegistry/LivenessVectorService with cleanup methods, updated tests.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-k8s-configmap-watch-and-unified-config/15-RESEARCH.md
@src/SnmpCollector/Pipeline/OidMapService.cs
@src/SnmpCollector/Pipeline/IOidMapService.cs
@src/SnmpCollector/Pipeline/DeviceRegistry.cs
@src/SnmpCollector/Pipeline/IDeviceRegistry.cs
@src/SnmpCollector/Pipeline/DeviceInfo.cs
@src/SnmpCollector/Pipeline/JobIntervalRegistry.cs
@src/SnmpCollector/Pipeline/IJobIntervalRegistry.cs
@src/SnmpCollector/Pipeline/LivenessVectorService.cs
@src/SnmpCollector/Pipeline/ILivenessVectorService.cs
@src/SnmpCollector/Configuration/OidMapOptions.cs
@src/SnmpCollector/Configuration/DevicesOptions.cs
@src/SnmpCollector/Configuration/DeviceOptions.cs
@src/SnmpCollector/Configuration/MetricPollOptions.cs
@tests/SnmpCollector.Tests/Pipeline/OidMapServiceTests.cs
@tests/SnmpCollector.Tests/Pipeline/DeviceRegistryTests.cs
@tests/SnmpCollector.Tests/Helpers/TestOptionsMonitor.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SimetraConfigModel and make OidMapService + DeviceRegistry mutable</name>
  <files>
    src/SnmpCollector/Configuration/SimetraConfigModel.cs
    src/SnmpCollector/Pipeline/OidMapService.cs
    src/SnmpCollector/Pipeline/IOidMapService.cs
    src/SnmpCollector/Pipeline/DeviceRegistry.cs
    src/SnmpCollector/Pipeline/IDeviceRegistry.cs
  </files>
  <action>
    1. Create `src/SnmpCollector/Configuration/SimetraConfigModel.cs`:
       ```csharp
       namespace SnmpCollector.Configuration;
       public sealed class SimetraConfigModel
       {
           public Dictionary<string, string> OidMap { get; set; } = new();
           public List<DeviceOptions> Devices { get; set; } = new();
       }
       ```
       This POCO maps directly to the unified JSON structure with "OidMap" and "Devices" sections.

    2. Refactor `OidMapService.cs` to remove `IOptionsMonitor<OidMapOptions>` dependency:
       - Change constructor to accept `Dictionary<string, string> initialEntries` and `ILogger<OidMapService>` (remove IOptionsMonitor parameter entirely).
       - Remove the `_changeToken` field and `IDisposable` implementation (no more OnChange subscription).
       - Remove the `OnOidMapChanged` private method.
       - Add a public `void UpdateMap(Dictionary<string, string> entries)` method that:
         - Builds a new FrozenDictionary from the entries.
         - Computes the diff (added, removed, changed) using the same logic currently in OnOidMapChanged.
         - Does the atomic `_map = newMap` volatile swap.
         - Logs the diff exactly as OnOidMapChanged currently does.
       - Constructor should call `BuildFrozenMap(initialEntries)` directly (not via OidMapOptions).
       - Keep Resolve(), EntryCount, BuildFrozenMap() unchanged.

    3. Update `IOidMapService.cs`:
       - Add `void UpdateMap(Dictionary<string, string> entries);` to the interface.
       - Keep existing Resolve and EntryCount members.

    4. Refactor `DeviceRegistry.cs` to support mutable reload:
       - Change `_byIp` and `_byName` from `readonly` to `volatile` (same pattern as OidMapService._map).
       - Keep the existing constructor that takes `IOptions<DevicesOptions>` -- it still works for initial startup loading via DI. The constructor body stays the same but assigns to volatile fields.
       - Add a public `async Task ReloadAsync(List<DeviceOptions> devices)` method that:
         - Builds new byIp and byName dictionaries from the device list.
         - Uses `Dns.GetHostAddressesAsync()` (not synchronous GetHostAddresses) for DNS resolution.
         - Converts MetricPolls to MetricPollInfo (same logic as constructor).
         - Does atomic volatile swap of both `_byIp` and `_byName`.
         - Logs: "DeviceRegistry reloaded: {DeviceCount} devices, +{Added} added, -{Removed} removed"
         - Returns the old and new device name sets so callers can determine adds/removes.
       - Change `AllDevices` property to read from volatile `_byName.Values` (it already works since FrozenDictionary is immutable once created).

    5. Update `IDeviceRegistry.cs`:
       - Add `Task ReloadAsync(List<DeviceOptions> devices);` to the interface.
       - Keep existing TryGetDevice, TryGetDeviceByName, AllDevices.
  </action>
  <verify>
    Run `dotnet build src/SnmpCollector/SnmpCollector.csproj` -- must compile without errors.
    The build will initially have errors in ServiceCollectionExtensions.cs (OidMapService constructor change) and test files -- that is expected and will be fixed in Task 2 and Plan 03.
    Verify the new files exist and contain the expected methods.
  </verify>
  <done>
    - SimetraConfigModel.cs exists with OidMap and Devices properties.
    - OidMapService no longer depends on IOptionsMonitor; has UpdateMap method with atomic swap + diff logging.
    - DeviceRegistry has volatile fields and ReloadAsync method with async DNS resolution.
    - IOidMapService has UpdateMap method.
    - IDeviceRegistry has ReloadAsync method.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add cleanup methods to registries and update all tests</name>
  <files>
    src/SnmpCollector/Pipeline/JobIntervalRegistry.cs
    src/SnmpCollector/Pipeline/IJobIntervalRegistry.cs
    src/SnmpCollector/Pipeline/LivenessVectorService.cs
    src/SnmpCollector/Pipeline/ILivenessVectorService.cs
    tests/SnmpCollector.Tests/Pipeline/OidMapServiceTests.cs
    tests/SnmpCollector.Tests/Pipeline/DeviceRegistryTests.cs
  </files>
  <action>
    1. Update `IJobIntervalRegistry.cs`:
       - Add `void Unregister(string jobKey);` method.
       - Add XML doc: "Removes the registered interval for a job key. Called when a poll job is removed during device config reload."

    2. Update `JobIntervalRegistry.cs`:
       - Add `public void Unregister(string jobKey) { _intervals.Remove(jobKey); }` implementation.

    3. Update `ILivenessVectorService.cs`:
       - Add `void Remove(string jobKey);` method.
       - Add XML doc: "Removes the liveness stamp for a job key. Called when a poll job is removed during device config reload."

    4. Update `LivenessVectorService.cs`:
       - Add `public void Remove(string jobKey) { _stamps.TryRemove(jobKey, out _); }` implementation.

    5. Update `tests/SnmpCollector.Tests/Pipeline/OidMapServiceTests.cs`:
       - The `CreateService` helper currently uses `TestOptionsMonitor<OidMapOptions>`. Change it to construct OidMapService with `new Dictionary<string, string>` directly (matching the new constructor signature).
       - Update the hot-reload tests (`Resolve_AfterReload_NewOidResolves`, `Resolve_AfterReload_RemovedOidReturnsUnknown`) to call `sut.UpdateMap(updatedEntries)` instead of `monitor.Change(...)`.
       - Remove the TestOptionsMonitor usage from this test file entirely.
       - All 6 existing test methods must continue to pass with the same assertions.

    6. Update `tests/SnmpCollector.Tests/Pipeline/DeviceRegistryTests.cs`:
       - The `CreateRegistry` helper uses `Options.Create(devicesOptions)`. This still works since the constructor still takes `IOptions<DevicesOptions>`. No changes needed to existing tests.
       - Add a new test: `ReloadAsync_AddsNewDevice_FoundByName` that:
        - Creates a registry with TwoDeviceOptions().
        - Calls `await sut.ReloadAsync(newDevices)` with a third device added.
        - Asserts TryGetDeviceByName finds the new device.
        - Asserts AllDevices.Count == 3.
       - Add a new test: `ReloadAsync_RemovesDevice_NotFoundByName` that:
        - Creates a registry with TwoDeviceOptions().
        - Calls ReloadAsync with only one device.
        - Asserts TryGetDeviceByName returns false for the removed device.
        - Asserts AllDevices.Count == 1.
  </action>
  <verify>
    Run `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj --filter "FullyQualifiedName~OidMapService|FullyQualifiedName~DeviceRegistry"` -- all tests must pass.
    Run `dotnet build src/SnmpCollector/SnmpCollector.csproj` -- must compile. Note: ServiceCollectionExtensions.cs will have a compile error because OidMapService constructor changed. This is expected and fixed in Plan 03.
  </verify>
  <done>
    - JobIntervalRegistry has Unregister method.
    - LivenessVectorService has Remove method.
    - OidMapServiceTests pass without TestOptionsMonitor, using UpdateMap for reload tests.
    - DeviceRegistryTests pass with new ReloadAsync tests proving add/remove works.
    - All existing test assertions unchanged.
  </done>
</task>

</tasks>

<verification>
- `dotnet test tests/SnmpCollector.Tests/SnmpCollector.Tests.csproj --filter "FullyQualifiedName~OidMapService|FullyQualifiedName~DeviceRegistry"` -- all tests green
- OidMapService.cs has no `using Microsoft.Extensions.Options` import
- DeviceRegistry.cs has `volatile` keyword on both dictionary fields
- IJobIntervalRegistry.cs contains `Unregister` method
- ILivenessVectorService.cs contains `Remove` method
</verification>

<success_criteria>
All registry services support runtime mutation via atomic swap or cleanup methods. Existing tests pass. New reload tests prove DeviceRegistry add/remove works. OidMapService is decoupled from IOptionsMonitor.
</success_criteria>

<output>
After completion, create `.planning/phases/15-k8s-configmap-watch-and-unified-config/15-01-SUMMARY.md`
</output>
