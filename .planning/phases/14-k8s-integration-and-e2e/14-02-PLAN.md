---
phase: 14-k8s-integration-and-e2e
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/k8s/configmap.yaml
  - deploy/k8s/simulators/configmap-devices.yaml
autonomous: true

must_haves:
  truths:
    - "simetra-config ConfigMap contains devices.json key with OBP-01 and NPB-01 device entries"
    - "OBP-01 device entry has all 24 OBP OIDs in a single MetricPoll with 10-second interval"
    - "NPB-01 device entry has all 68 NPB OIDs in a single MetricPoll with 10-second interval"
    - "Device addresses use K8s Service DNS names, not placeholder IPs"
    - "Each device entry has explicit CommunityString field"
  artifacts:
    - path: "deploy/k8s/configmap.yaml"
      provides: "devices.json key in simetra-config ConfigMap"
      contains: "devices.json"
    - path: "deploy/k8s/configmap.yaml"
      provides: "OBP-01 with 24 OIDs and NPB-01 with 68 OIDs"
      contains: "obp-simulator.simetra.svc.cluster.local"
  key_links:
    - from: "deploy/k8s/configmap.yaml (devices.json key)"
      to: "deploy/k8s/configmap.yaml (oidmap-obp.json key)"
      via: "OID strings in MetricPoll.Oids must match keys in OidMap"
      pattern: "1\\.3\\.6\\.1\\.4\\.1\\.47477"
    - from: "deploy/k8s/configmap.yaml (devices.json key)"
      to: "src/SnmpCollector/Configuration/DeviceOptions.cs"
      via: "JSON property names must match C# model property names"
      pattern: "Name.*IpAddress.*Port.*CommunityString.*MetricPolls"
---

<objective>
Create the devices.json ConfigMap key containing MetricPoll groups for OBP-01 (24 OIDs) and NPB-01 (68 OIDs) with K8s Service DNS addresses and explicit community strings.

Purpose: The snmp-collector needs device configuration to know which simulators to poll and which OIDs to request. This is the data that drives the entire metric pipeline.

Output: Updated ConfigMap YAML with devices.json key containing all 92 OIDs across two device entries.
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-k8s-integration-and-e2e/14-CONTEXT.md

@deploy/k8s/configmap.yaml
@deploy/k8s/deployment.yaml
@deploy/k8s/simulators/configmap-devices.yaml
@src/SnmpCollector/Configuration/DeviceOptions.cs
@src/SnmpCollector/Configuration/MetricPollOptions.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add devices.json key to simetra-config ConfigMap</name>
  <files>deploy/k8s/configmap.yaml</files>
  <action>
Add a `devices.json` key to the existing `simetra-config` ConfigMap in `deploy/k8s/configmap.yaml`. This key sits alongside `appsettings.k8s.json`, `oidmap-obp.json`, and `oidmap-npb.json`.

The devices.json content must:

1. Have a top-level `"Devices"` array (matches DevicesOptions.SectionName binding).

2. OBP-01 entry:
   - `"Name": "OBP-01"`
   - `"IpAddress": "obp-simulator.simetra.svc.cluster.local"` (K8s Service DNS)
   - `"Port": 161`
   - `"CommunityString": "Simetra.OBP-01"` (explicit community string)
   - One MetricPoll with `"IntervalSeconds": 10` and `"Oids"` array containing ALL 24 OBP OIDs from the oidmap-obp.json key in the same ConfigMap. Copy the exact OID strings.

3. NPB-01 entry:
   - `"Name": "NPB-01"`
   - `"IpAddress": "npb-simulator.simetra.svc.cluster.local"` (K8s Service DNS)
   - `"Port": 161`
   - `"CommunityString": "Simetra.NPB-01"` (explicit community string)
   - One MetricPoll with `"IntervalSeconds": 10` and `"Oids"` array containing ALL 68 NPB OIDs from the oidmap-npb.json key in the same ConfigMap. Copy the exact OID strings.

4. Remove the empty `"Devices": []` from `appsettings.k8s.json` key since devices.json now provides it (would conflict / override).

The MetricPollOptions model only has `Oids` (List of string) and `IntervalSeconds`. Each OID in the Oids array is a plain string like `"1.3.6.1.4.1.47477.10.21.1.3.1.0"`. The pipeline resolves metric types from SNMP response TypeCode at runtime, not from configuration.

Cross-check: Count OBP OIDs = 24 (4 links x 6 OIDs per link: link_state, channel, r1_power, r2_power, r3_power, r4_power). Count NPB OIDs = 68 (4 system health + 8 ports x 8 per-port OIDs). Verify counts match.

**Deployment.yaml verification:** Confirm `deploy/k8s/deployment.yaml` already has CONFIG_DIRECTORY=/app/config env var and mounts the config volume at /app/config WITHOUT subPath. If this is already correct (it should be), no changes needed. If subPath is present or CONFIG_DIRECTORY is missing, fix it.
  </action>
  <verify>
Validate YAML syntax: `python -c "import yaml; yaml.safe_load(open('deploy/k8s/configmap.yaml'))"` (or equivalent). Count OIDs: grep for OID pattern `1.3.6.1.4.1.47477` in the devices.json section and verify 92 total (24 OBP + 68 NPB). Verify `deploy/k8s/deployment.yaml` has CONFIG_DIRECTORY=/app/config and no subPath on the config mount.
  </verify>
  <done>ConfigMap has devices.json key with OBP-01 (24 OIDs) and NPB-01 (68 OIDs), DNS addresses, explicit CommunityString, 10s interval. YAML is valid. Deployment.yaml confirmed correct for directory mount.</done>
</task>

<task type="auto">
  <name>Task 2: Remove obsolete configmap-devices.yaml template</name>
  <files>deploy/k8s/simulators/configmap-devices.yaml</files>
  <action>
Delete `deploy/k8s/simulators/configmap-devices.yaml`. This file is the old template with PLACEHOLDER_*_POD_IP values and the wrong MetricPoll structure (MetricName/MetricType/Oids[].Oid pattern that doesn't match the C# model). It has been superseded by the devices.json key in the main ConfigMap.

Use `git rm deploy/k8s/simulators/configmap-devices.yaml` to remove it from tracking.
  </action>
  <verify>
Confirm file is deleted: `ls deploy/k8s/simulators/configmap-devices.yaml` should fail. Confirm no other YAML files reference `configmap-devices`: grep for `configmap-devices` across deploy/ directory.
  </verify>
  <done>Obsolete configmap-devices.yaml template is removed. No dangling references.</done>
</task>

</tasks>

<verification>
1. `deploy/k8s/configmap.yaml` has `devices.json` key with valid JSON
2. OBP-01 entry has exactly 24 OIDs matching oidmap-obp.json
3. NPB-01 entry has exactly 68 OIDs matching oidmap-npb.json
4. Both entries use K8s Service DNS names for IpAddress
5. Both entries have explicit CommunityString
6. Old configmap-devices.yaml is removed
7. No other files reference the removed template
8. deployment.yaml has CONFIG_DIRECTORY=/app/config and directory mount (no subPath)
</verification>

<success_criteria>
- simetra-config ConfigMap devices.json key contains 2 device entries with all 92 OIDs
- Device addresses are K8s Service DNS names
- MetricPoll structure matches C# model (plain string OID list, IntervalSeconds)
- Old placeholder-based template is removed
- deployment.yaml confirmed correct for config directory mount
</success_criteria>

<output>
After completion, create `.planning/phases/14-k8s-integration-and-e2e/14-02-SUMMARY.md`
</output>
