---
phase: 13-simulator-refinement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - simulators/npb/npb_simulator.py
autonomous: true

must_haves:
  truths:
    - "NPB simulator responds to SNMP GET for all 68 OIDs defined in oidmap-npb.json"
    - "NPB simulator uses Simetra.NPB-01 community string by default and rejects other communities"
    - "Counter64 OIDs increment per traffic profile (heavy/medium/light/zero)"
    - "System health OIDs (cpu, mem, temp) random-walk as OctetString; uptime increments as OctetString"
    - "Port link traps fire for P1-P3 and P5-P7 with correct trap OID and status varbind"
    - "P4 and P8 remain permanently down but still respond to GET with valid values"
  artifacts:
    - path: "simulators/npb/npb_simulator.py"
      provides: "Complete NPB SNMP agent with 68 poll OIDs, counter increments, system health walk, and port link traps"
      min_lines: 350
  key_links:
    - from: "simulators/npb/npb_simulator.py"
      to: "src/SnmpCollector/config/oidmap-npb.json"
      via: "1:1 OID string match"
      pattern: "47477\\.100\\.[12]\\."
---

<objective>
Clean rewrite the NPB simulator to serve exactly 68 OIDs matching oidmap-npb.json, with Simetra.NPB-01 community string, realistic traffic profiles on Counter64 counters, OctetString system health metrics, and portLinkChange traps for active ports.

Purpose: The current NPB simulator serves ~560 OIDs from the old 47477.100.4.* tree. This rewrite replaces the entire OID tree with the new 47477.100.1.* (system) and 47477.100.2.* (per-port) structure, exactly matching oidmap-npb.json.
Output: Rewritten simulators/npb/npb_simulator.py (~400-500 lines)
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-simulator-refinement/13-CONTEXT.md
@.planning/phases/13-simulator-refinement/13-RESEARCH.md
@src/SnmpCollector/config/oidmap-npb.json
@simulators/npb/npb_simulator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite NPB simulator with 68 OIDs, traffic profiles, and system health</name>
  <files>simulators/npb/npb_simulator.py</files>
  <action>
  Clean rewrite of npb_simulator.py. Reuse proven patterns (DynamicInstance, oid_str_to_tuple, supervised_task, resolve_trap_targets, random_walk_int) from the existing codebase. The old 47477.100.4.* tree is completely discarded.

  **Constants and config:**
  - NPB_PREFIX = "1.3.6.1.4.1.47477.100"
  - COUNTER64_MAX = 18_446_744_073_709_551_615
  - DEVICE_NAME from env (default "NPB-01")
  - COMMUNITY from env (default f"Simetra.{DEVICE_NAME}")
  - TRAP_TARGET, TRAP_PORT from env (same as existing)
  - TRAP_INTERVAL_MIN = 60, TRAP_INTERVAL_MAX = 300 (for per-port trap loops)
  - COUNTER_UPDATE_INTERVAL = 10 (seconds between counter increments)
  - HEALTH_UPDATE_INTERVAL = 10 (seconds between system health walks)
  - STARTUP_DELAY = 12

  **Community string:**
  - config.add_v1_system(snmpEngine, "my-area", COMMUNITY) -- NOT "public"
  - config.add_vacm_user(snmpEngine, 2, "my-area", "noAuthNoPriv", (1, 3, 6, 1, 4, 1))
  - Trap sender: CommunityData(COMMUNITY)
  - Log the community string at startup

  **State structure:**
  ```python
  # System health state (OctetString values)
  system_state = {
      "cpu_util": "15.0",
      "mem_util": "45.0",
      "sys_temp": "42.0",
      "uptime": "0",
  }

  # Per-port state
  port_states = {}
  for p in range(1, 9):
      port_states[p] = {
          "port_status": 1 if p not in (4, 8) else 2,  # P4, P8 start down
          "rx_octets": 0, "tx_octets": 0,
          "rx_packets": 0, "tx_packets": 0,
          "rx_errors": 0, "tx_errors": 0,
          "rx_drops": 0,
      }
  ```

  **OID registration -- System metrics (4 OIDs):**
  OID pattern: f"{NPB_PREFIX}.1.{metricId}.0"
  ```
  SYSTEM_METRICS = [
      (1, "cpu_util",  v2c.OctetString),
      (2, "mem_util",  v2c.OctetString),
      (3, "sys_temp",  v2c.OctetString),
      (4, "uptime",    v2c.OctetString),
  ]
  ```
  Register MibScalar + DynamicInstance for each. Getter reads from system_state dict.

  **CRITICAL TYPE NOTE:** ALL four system metrics are OctetString per the OID map (source of truth). The random walk values from CONTEXT.md apply, but are stored/returned as string representations (e.g., "15.0" for cpu, "45.0" for mem, "42.0" for temp, "0" for uptime). The cpu/mem/temp values use one decimal place; uptime is integer seconds as string.

  **OID registration -- Per-port metrics (64 OIDs = 8 ports x 8 metrics):**
  OID pattern: f"{NPB_PREFIX}.2.{port}.{metricId}.0"
  ```
  PORT_METRICS = [
      (1, "port_status",  v2c.Integer32),
      (2, "rx_octets",    v2c.Counter64),
      (3, "tx_octets",    v2c.Counter64),
      (4, "rx_packets",   v2c.Counter64),
      (5, "tx_packets",   v2c.Counter64),
      (6, "rx_errors",    v2c.Counter64),
      (7, "tx_errors",    v2c.Counter64),
      (8, "rx_drops",     v2c.Counter64),
  ]
  ```
  Loop over ports 1-8 and PORT_METRICS. Register MibScalar + DynamicInstance for each. Getter reads from port_states[port][state_key]. Total: 4 + 64 = 68 OIDs.

  **Traffic profiles:**
  ```python
  TRAFFIC_PROFILES = {
      "heavy":  {"octet_range": (500_000, 2_000_000), "packet_range": (1000, 5000)},
      "medium": {"octet_range": (100_000, 500_000),   "packet_range": (200, 1000)},
      "light":  {"octet_range": (10_000, 50_000),     "packet_range": (50, 200)},
      "zero":   {"octet_range": (0, 0),               "packet_range": (0, 0)},
  }
  PORT_TRAFFIC = {1: "heavy", 2: "heavy", 3: "light", 4: "zero",
                  5: "medium", 6: "medium", 7: "heavy", 8: "zero"}
  ```

  **Background tasks:**

  1. `increment_counters()` -- every COUNTER_UPDATE_INTERVAL seconds:
     - For each port, look up traffic profile
     - Add random increment within profile range to rx_octets, tx_octets (use slightly lower tx than rx for realism, e.g., tx = rx * 0.8-0.95)
     - Add random increment within packet range to rx_packets, tx_packets
     - Error/drop injection: if profile != "zero" and random.random() < 0.01, add random.randint(1, 3) to rx_errors or tx_errors or rx_drops (pick one randomly)
     - All counter updates use modular arithmetic: (current + increment) % (COUNTER64_MAX + 1)
     - Skip ports with port_status == 2 for counter increments (down ports don't generate traffic), BUT P4/P8 counters remain at their current values (0) -- they are registered and respond to GET.

  2. `update_system_health()` -- every HEALTH_UPDATE_INTERVAL seconds:
     - cpu_util: random walk the float value 5.0-40.0, step 2.0, store as f"{value:.1f}"
     - mem_util: random walk the float value 30.0-70.0, step 1.5, store as f"{value:.1f}"
     - sys_temp: random walk the float value 35.0-55.0, step 1.0, store as f"{value:.1f}"
     - uptime: increment by HEALTH_UPDATE_INTERVAL, store as str(int_value)
     - Use a float-based random_walk for cpu/mem/temp:
       ```python
       def random_walk_float(current, step, low, high):
           delta = random.uniform(-step, step)
           return max(low, min(high, current + delta))
       ```
     - Keep internal float tracking variables for cpu/mem/temp; system_state stores the string representation.

  3. `per_port_trap_loop(port)` x6 -- for ports P1-P3, P5-P7 only (P4, P8 stay permanently down):
     - Wait STARTUP_DELAY
     - Loop: sleep random(TRAP_INTERVAL_MIN, TRAP_INTERVAL_MAX)
     - Toggle port_status: 1 <-> 2 in port_states[port]
     - Send portLinkChange trap
     - Trap OID: f"{NPB_PREFIX}.3.{port}.0"
     - Varbind: port_status poll OID (f"{NPB_PREFIX}.2.{port}.1.0") with new status as Integer32 (1=up, 2=down)
     - Use CommunityData(COMMUNITY)

  **Shutdown:**
  Graceful shutdown with signal handlers cancelling all tasks and closing dispatcher.

  **Startup log:**
  Log: community string, 68 registered OIDs (4 system + 64 per-port), traffic profiles, trap target/port, initial port statuses.
  </action>
  <verify>
  1. Run `python -c "import ast; ast.parse(open('simulators/npb/npb_simulator.py').read()); print('Syntax OK')"` to verify no syntax errors.
  2. Count OID registrations: 4 SYSTEM_METRICS + 8 PORT_METRICS x 8 ports = 68.
  3. Grep for "public" in the file -- should NOT appear anywhere.
  4. Grep for "47477.100.4" -- should NOT appear (old tree completely removed).
  5. Verify every OID string from oidmap-npb.json has a corresponding registration by cross-referencing the prefix + metricId pattern.
  6. Verify system metrics use v2c.OctetString (not Integer32).
  </verify>
  <done>
  - npb_simulator.py is a clean rewrite, ~400-500 lines
  - 68 OIDs registered (4 system OctetString + 8 ports x 8 metrics including Counter64)
  - Community string defaults to Simetra.NPB-01, configurable via COMMUNITY env var
  - Counter64 values increment per traffic profile with modular arithmetic wrapping
  - System health OIDs random-walk as OctetString representations
  - portLinkChange traps fire for P1-P3, P5-P7 with status varbind
  - P4 and P8 remain down, counters at 0, but all OIDs respond to GET
  - No reference to "public" or old "47477.100.4" tree anywhere in the file
  </done>
</task>

</tasks>

<verification>
- Python syntax check passes
- File contains no references to old OID tree (47477.100.4)
- Community string uses COMMUNITY variable everywhere (agent + trap sender)
- System metrics registered as OctetString (not Integer32)
- Counter64 values use modular arithmetic wrapping
- All 68 poll OIDs match entries in oidmap-npb.json
- Trap OIDs follow pattern: 47477.100.3.{port}.0
- P4 and P8 OIDs registered but stay at down/zero values
</verification>

<success_criteria>
NPB simulator serves 68 OIDs matching oidmap-npb.json exactly, uses Simetra.NPB-01 community, increments counters per traffic profile, walks system health as OctetString, and sends portLinkChange traps with correct varbinds.
</success_criteria>

<output>
After completion, create `.planning/phases/13-simulator-refinement/13-02-SUMMARY.md`
</output>
