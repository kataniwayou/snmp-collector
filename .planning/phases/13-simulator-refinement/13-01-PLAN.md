---
phase: 13-simulator-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - simulators/obp/obp_simulator.py
autonomous: true

must_haves:
  truths:
    - "OBP simulator responds to SNMP GET for all 24 OIDs defined in oidmap-obp.json"
    - "OBP simulator uses Simetra.OBP-01 community string by default and rejects other communities"
    - "Power OIDs return Integer32 values that random-walk within -50 to -200 (tenths of dBm)"
    - "StateChange traps fire for all 4 links with correct trap OID and channel varbind"
    - "Each link has distinguishable power baselines across R1-R4 receivers"
  artifacts:
    - path: "simulators/obp/obp_simulator.py"
      provides: "Complete OBP SNMP agent with 24 poll OIDs, power random walk, and StateChange traps"
      min_lines: 220
  key_links:
    - from: "simulators/obp/obp_simulator.py"
      to: "src/SnmpCollector/config/oidmap-obp.json"
      via: "1:1 OID string match"
      pattern: "47477\\.10\\.21\\.[1-4]\\.3\\.(1|4|10|11|12|13)\\.0"
---

<objective>
Clean rewrite the OBP simulator to serve exactly 24 OIDs matching oidmap-obp.json, with Simetra.OBP-01 community string, power random walk on R1-R4 receivers, and StateChange traps for all 4 links.

Purpose: The current OBP simulator only serves 8 OIDs (link_state + channel). This rewrite adds 16 power OIDs (4 links x 4 receivers) and switches to the Simetra.{DeviceName} community string convention.
Output: Rewritten simulators/obp/obp_simulator.py (~250-300 lines)
</objective>

<execution_context>
@C:\Users\UserL\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\UserL\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-simulator-refinement/13-CONTEXT.md
@.planning/phases/13-simulator-refinement/13-RESEARCH.md
@src/SnmpCollector/config/oidmap-obp.json
@simulators/obp/obp_simulator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite OBP simulator with 24 OIDs, power walk, and community string</name>
  <files>simulators/obp/obp_simulator.py</files>
  <action>
  Clean rewrite of obp_simulator.py. Reuse the proven DynamicInstance, oid_str_to_tuple, supervised_task, resolve_trap_targets patterns from the existing file. The structure:

  **Constants and config:**
  - BYPASS_PREFIX = "1.3.6.1.4.1.47477.10.21"
  - DEVICE_NAME from env (default "OBP-01")
  - COMMUNITY from env (default f"Simetra.{DEVICE_NAME}")
  - TRAP_TARGET, TRAP_PORT, TRAP_INTERVAL_MIN, TRAP_INTERVAL_MAX from env (same defaults as current)
  - STARTUP_DELAY = 12

  **Community string:**
  - config.add_v1_system(snmpEngine, "my-area", COMMUNITY) -- NOT "public"
  - config.add_vacm_user(snmpEngine, 2, "my-area", "noAuthNoPriv", (1, 3, 6, 1, 4, 1))
  - Trap sender: CommunityData(COMMUNITY) -- NOT "public"
  - Log the community string at startup

  **OID registration (24 OIDs, data-driven):**
  Define LINK_METRICS table:
  ```
  LINK_METRICS = [
      (1,  "link_state", v2c.Integer32),
      (4,  "channel",    v2c.Integer32),
      (10, "r1_power",   v2c.Integer32),
      (11, "r2_power",   v2c.Integer32),
      (12, "r3_power",   v2c.Integer32),
      (13, "r4_power",   v2c.Integer32),
  ]
  ```
  Loop over links 1-4 and LINK_METRICS to register MibScalar + DynamicInstance pairs.
  OID pattern: f"{BYPASS_PREFIX}.{link}.3.{suffix}"
  Each DynamicInstance gets a closure reading from link_states[link][state_key].

  **Initial state:**
  ```
  link_states = {
      1: {"link_state": 1, "channel": 1, "r1_power": -85, "r2_power": -92, "r3_power": -88, "r4_power": -95},
      2: {"link_state": 1, "channel": 1, "r1_power": -78, "r2_power": -82, "r3_power": -90, "r4_power": -87},
      3: {"link_state": 1, "channel": 0, "r1_power": -110, "r2_power": -105, "r3_power": -115, "r4_power": -108},
      4: {"link_state": 0, "channel": 1, "r1_power": -130, "r2_power": -140, "r3_power": -125, "r4_power": -135},
  }
  ```
  These baselines are intentionally spread across different ranges so each link is distinguishable on a dashboard.

  **Background tasks:**

  1. `update_power_values()` -- runs every 10 seconds. For each link 1-4, for each of r1_power..r4_power, apply random_walk_int(current, step=2, low=-200, high=-50). Use the existing random_walk_int pattern.

  2. `per_link_trap_loop(link_num)` x4 -- same pattern as existing. Wait STARTUP_DELAY, then loop: sleep random(TRAP_INTERVAL_MIN, TRAP_INTERVAL_MAX), toggle channel (0<->1), update link_states, send StateChange trap.

  **Trap sending:**
  - Trap OID: f"{BYPASS_PREFIX}.{link}.3.50.2"
  - Varbind: channel poll OID (f"{BYPASS_PREFIX}.{link}.3.4.0") with new channel value as Integer32
  - Use CommunityData(COMMUNITY)

  **Shutdown:**
  Keep the existing graceful shutdown pattern with signal handlers cancelling tasks and closing dispatcher.

  **Startup log:**
  Log: community string, 24 registered OIDs, initial link states, trap target/port.

  **CRITICAL: Verify OID string match.** Every registered OID must exactly match an entry in oidmap-obp.json. The 24 OIDs are:
  - Links 1-4 x suffixes 1, 4, 10, 11, 12, 13 = 24 entries
  - Pattern: 1.3.6.1.4.1.47477.10.21.{link}.3.{suffix}.0
  - The .0 index is added by DynamicInstance via index_tuple=(0,), NOT in the oid_tuple passed to MibScalar.
  </action>
  <verify>
  1. Run `python -c "import ast; ast.parse(open('simulators/obp/obp_simulator.py').read()); print('Syntax OK')"` to verify no syntax errors.
  2. Count registered OIDs in the code -- grep for the LINK_METRICS definition and verify 6 metrics x 4 links = 24.
  3. Grep for "public" in the file -- should NOT appear anywhere (community string must be Simetra.{DeviceName}).
  4. Verify every OID string from oidmap-obp.json has a corresponding registration in the simulator by cross-referencing the BYPASS_PREFIX + link + suffix pattern.
  </verify>
  <done>
  - obp_simulator.py is a clean rewrite, ~250-300 lines
  - 24 OIDs registered (4 links x 6 metrics: link_state, channel, r1-r4 power)
  - Community string defaults to Simetra.OBP-01, configurable via COMMUNITY env var
  - Power values random-walk within [-200, -50] with step=2 every 10s
  - StateChange traps fire per-link with channel varbind using correct community
  - No reference to "public" community anywhere in the file
  </done>
</task>

</tasks>

<verification>
- Python syntax check passes
- File contains exactly 6 entries in LINK_METRICS (suffixes 1, 4, 10, 11, 12, 13)
- Loop registers for links 1-4, producing 24 OID registrations
- Community string uses COMMUNITY variable everywhere (agent + trap sender)
- Trap OIDs match pattern: 47477.10.21.{1-4}.3.50.2
- All 24 poll OIDs match entries in oidmap-obp.json
</verification>

<success_criteria>
OBP simulator serves 24 OIDs matching oidmap-obp.json exactly, uses Simetra.OBP-01 community, walks power values, and sends StateChange traps with correct varbinds.
</success_criteria>

<output>
After completion, create `.planning/phases/13-simulator-refinement/13-01-SUMMARY.md`
</output>
